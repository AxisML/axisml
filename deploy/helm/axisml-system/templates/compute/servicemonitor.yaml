{{- if and .Values.compute.enabled .Values.compute.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "axisml.fullname" . }}-compute
  labels:
    {{- include "axisml.componentLabels" (dict "context" . "component" "compute") | nindent 4 }}
    {{- with .Values.compute.serviceMonitor.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    matchLabels:
      {{- include "axisml.componentSelectorLabels" (dict "context" . "component" "compute") | nindent 6 }}
  endpoints:
    - port: metrics
      path: /metrics
      interval: {{ .Values.compute.serviceMonitor.interval | default "30s" }}
{{- end }}
