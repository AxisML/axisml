{{- if .Values.compute.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "axisml.fullname" . }}-compute
  labels:
    {{- include "axisml.componentLabels" (dict "context" . "component" "compute") | nindent 4 }}
data:
  DATABASE_HOST: {{ include "axisml.databaseHost" . | quote }}
  DATABASE_PORT: {{ include "axisml.databasePort" . | quote }}
  DATABASE_NAME: {{ .Values.database.auth.database | quote }}
  DATABASE_USER: {{ .Values.database.auth.username | quote }}
  API_BIND_ADDRESS: {{ printf ":%d" (int .Values.compute.service.port) | quote }}
  PROBES_BIND_ADDRESS: {{ printf ":%d" (int .Values.compute.probesPort) | quote }}
  METRICS_BIND_ADDRESS: {{ printf ":%d" (int .Values.compute.metricsPort) | quote }}
  LEADER_ELECT: {{ .Values.compute.leaderElection.enabled | quote }}
  LEADER_NAMESPACE: {{ .Release.Namespace | quote }}
  RECONCILE_INTERVAL: {{ .Values.compute.reconcileInterval | quote }}
  LOG_DEVELOPMENT: {{ .Values.compute.logDevelopment | quote }}
  BOOTSTRAP_POOL: {{ .Values.compute.bootstrap.defaultPool | quote }}
  {{- range $key, $value := .Values.compute.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
