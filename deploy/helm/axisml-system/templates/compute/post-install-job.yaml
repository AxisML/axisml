{{- if and .Values.compute.enabled .Values.compute.bootstrap.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "axisml.fullname" . }}-compute-bootstrap
  labels:
    {{- include "axisml.componentLabels" (dict "context" . "component" "compute-bootstrap") | nindent 4 }}
  annotations:
    "helm.sh/hook": post-install,post-upgrade
    "helm.sh/hook-weight": "5"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 5
  template:
    metadata:
      labels:
        {{- include "axisml.componentLabels" (dict "context" . "component" "compute-bootstrap") | nindent 8 }}
    spec:
      restartPolicy: OnFailure
      serviceAccountName: {{ include "axisml.fullname" . }}-compute
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: bootstrap
          image: {{ include "axisml.image" (dict "imageRoot" .Values.compute.image "global" .Values.global "chart" .Chart) }}
          imagePullPolicy: {{ .Values.compute.image.pullPolicy }}
          args: ["bootstrap"]
          envFrom:
            - configMapRef:
                name: {{ include "axisml.fullname" . }}-compute
          env:
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "axisml.fullname" . }}-compute-db
                  key: password
{{- end }}
