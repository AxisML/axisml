{{- if .Values.catalog.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "axisml.fullname" . }}-catalog
  labels:
    {{- include "axisml.componentLabels" (dict "context" . "component" "catalog") | nindent 4 }}
spec:
  replicas: {{ .Values.catalog.replicaCount }}
  selector:
    matchLabels:
      {{- include "axisml.componentSelectorLabels" (dict "context" . "component" "catalog") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "axisml.componentLabels" (dict "context" . "component" "catalog") | nindent 8 }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: catalog
          image: {{ include "axisml.image" (dict "imageRoot" .Values.catalog.image "global" .Values.global "chart" .Chart) }}
          imagePullPolicy: {{ .Values.catalog.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.catalog.service.port }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: {{ include "axisml.fullname" . }}-catalog
          resources:
            {{- toYaml .Values.catalog.resources | nindent 12 }}
{{- end }}
