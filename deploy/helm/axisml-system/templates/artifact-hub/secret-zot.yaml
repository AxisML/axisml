{{- if and .Values.artifactHub.enabled (not .Values.artifactHub.storage.oci.adminSecretRef.name) }}
{{/*
zot admin credentials. Only rendered when adminSecretRef.name is empty so
the chart provides a default; otherwise the Deployment references an
externally-managed Secret with the same key shape.

Password has no default — `required` fails the install with a clear error
if the operator forgets to supply one. To skip this (demo without zot),
either set artifactHub.enabled=false or point adminSecretRef.name at an
externally-managed Secret.
*/}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "axisml.fullname" . }}-artifact-hub-zot
  labels:
    {{- include "axisml.componentLabels" (dict "context" . "component" "artifact-hub") | nindent 4 }}
type: Opaque
stringData:
  username: {{ .Values.artifactHub.storage.oci.adminSecretRef.username | default "admin" | quote }}
  password: {{ required "artifactHub.storage.oci.adminSecretRef.password is required (or set adminSecretRef.name to point at an externally-managed Secret)" .Values.artifactHub.storage.oci.adminSecretRef.password | quote }}
{{- end }}
