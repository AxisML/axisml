{{- if .Values.artifactHub.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "axisml.fullname" . }}-artifact-hub
  labels:
    {{- include "axisml.componentLabels" (dict "context" . "component" "artifact-hub") | nindent 4 }}
spec:
  replicas: {{ .Values.artifactHub.replicaCount }}
  selector:
    matchLabels:
      {{- include "axisml.componentSelectorLabels" (dict "context" . "component" "artifact-hub") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "axisml.componentLabels" (dict "context" . "component" "artifact-hub") | nindent 8 }}
    spec:
      serviceAccountName: {{ include "axisml.fullname" . }}-artifact-hub
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      initContainers:
        {{- include "axisml.waitForDatabase" . | nindent 8 }}
      containers:
        - name: artifact-hub
          image: {{ include "axisml.image" (dict "imageRoot" .Values.artifactHub.image "global" .Values.global "chart" .Chart) }}
          imagePullPolicy: {{ .Values.artifactHub.image.pullPolicy }}
          args: ["serve"]
          ports:
            - name: http
              containerPort: {{ .Values.artifactHub.service.port }}
              protocol: TCP
            - name: metrics
              containerPort: {{ .Values.artifactHub.metricsPort }}
              protocol: TCP
            - name: probes
              containerPort: {{ .Values.artifactHub.probesPort }}
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /etc/axisml/config.yaml
              subPath: config.yaml
              readOnly: true
          env:
            - name: AXISML_DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "axisml.fullname" . }}-artifact-hub-db
                  key: password
            - name: AXISML_OCI_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (printf "%s-artifact-hub-zot" (include "axisml.fullname" .)) .Values.artifactHub.storage.oci.adminSecretRef.name }}
                  key: {{ .Values.artifactHub.storage.oci.adminSecretRef.passwordKey }}
            {{- range $key, $value := .Values.artifactHub.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: probes
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: probes
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.artifactHub.resources | nindent 12 }}
      volumes:
        - name: config
          configMap:
            name: {{ include "axisml.fullname" . }}-artifact-hub
{{- end }}
