# Code generated by cmd/openapi-gen. DO NOT EDIT.
# Regenerate with: make doc-gen
components:
  schemas:
    Capabilities:
      properties:
        components:
          additionalProperties: {}
          type: object
      required:
      - components
      type: object
info:
  description: 'Composition surface of axisml-core (AxisML Lite single-host form):
    Cluster Manager + Compute Service + Artifact Hub served on one HTTP server, plus
    the Standalone Docker runtime. The per-resource contracts are identical to the
    System APIs (axisml-system/docs/apis/{cluster-manager,compute-service,artifact-hub}.yaml)
    and reachable at the same paths on this server; this document covers the probes
    and the Lite-owned aggregate capability endpoint.'
  title: AxisML Core API
  version: 0.0.0-dev
openapi: 3.0.3
paths:
  /api/v1/capabilities:
    get:
      operationId: getCapabilities
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Capabilities'
          description: Aggregate capability document.
      summary: Get the aggregate capability document
      tags:
      - Capabilities
  /healthz:
    get:
      operationId: healthz
      responses:
        "200":
          content:
            text/plain:
              schema:
                type: string
          description: ok
      summary: Liveness probe
      tags:
      - Health
  /readyz:
    get:
      operationId: readyz
      responses:
        "200":
          content:
            text/plain:
              schema:
                type: string
          description: ok
        "503":
          content:
            text/plain:
              schema:
                type: string
          description: dependency not yet ready
      summary: Readiness probe
      tags:
      - Health
servers:
- description: Same-origin (axisml-core)
  url: /
tags:
- description: Aggregate deployment-form capability document — the three System modules'
    per-form documents folded under their component key (design §5.5).
  name: Capabilities
- description: Liveness and readiness probes.
  name: Health
