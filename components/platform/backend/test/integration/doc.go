// Package integration_test holds the platform-backend integration suite.
// Tests are gated by `//go:build integration`; this untagged file lets the
// package compile cleanly under a plain `go test ./...`.
package integration_test
