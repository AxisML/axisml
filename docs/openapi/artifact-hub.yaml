# Code generated by cmd/openapi-gen. DO NOT EDIT.
# Regenerate with: make doc-gen
components:
  schemas:
    ArtifactCompleteInput:
      properties:
        digest:
          type: string
      required:
      - digest
      type: object
    ArtifactInitiateInput:
      properties:
        annotations:
          additionalProperties:
            type: string
          type: object
        description:
          type: string
        display_name:
          type: string
        labels:
          additionalProperties:
            type: string
          type: object
        spec:
          additionalProperties: {}
          type: object
        version:
          maxLength: 64
          minLength: 1
          pattern: ^[a-zA-Z0-9._-]+$
          type: string
      required:
      - spec
      - version
      type: object
    ArtifactInitiateResult:
      properties:
        artifact_id:
          format: uuid
          type: string
        expires_at:
          format: date-time
          type: string
        storage_kind:
          type: string
        upload_credentials:
          $ref: '#/components/schemas/OciCredentials'
        uri:
          type: string
      required:
      - artifact_id
      - expires_at
      - storage_kind
      - upload_credentials
      - uri
      type: object
    ArtifactList:
      properties:
        items:
          items:
            $ref: '#/components/schemas/ArtifactView'
          type: array
        total:
          format: int64
          type: integer
      required:
      - items
      - total
      type: object
    ArtifactResolveResult:
      properties:
        digest:
          type: string
        expires_at:
          format: date-time
          nullable: true
          type: string
        pull_credentials:
          allOf:
          - $ref: '#/components/schemas/OciCredentials'
          nullable: true
        storage_kind:
          type: string
        uri:
          type: string
      required:
      - storage_kind
      - uri
      type: object
    ArtifactView:
      properties:
        annotations:
          additionalProperties:
            type: string
          type: object
        created_at:
          format: date-time
          type: string
        description:
          type: string
        digest:
          type: string
        display_name:
          type: string
        id:
          format: uuid
          type: string
        kind:
          type: string
        labels:
          additionalProperties:
            type: string
          type: object
        message:
          type: string
        name:
          type: string
        namespace:
          type: string
        owner_user:
          type: string
        ready_at:
          format: date-time
          nullable: true
          type: string
        spec:
          additionalProperties: {}
          type: object
        status:
          type: string
        updated_at:
          format: date-time
          type: string
        version:
          type: string
      required:
      - created_at
      - id
      - kind
      - name
      - namespace
      - spec
      - status
      - updated_at
      - version
      type: object
    OciCredentials:
      properties:
        expires_at:
          format: date-time
          type: string
        password:
          type: string
        username:
          type: string
      required:
      - expires_at
      - password
      - username
      type: object
    Problem:
      properties:
        code:
          description: Discrete business error class.
          enum:
          - validation_failed
          - not_found
          - conflict
          - precondition_failed
          - unauthorized
          - forbidden
          - service_unavailable
          - internal_error
          - gone
          type: string
        detail:
          type: string
        details:
          additionalProperties: {}
          type: object
        instance:
          type: string
        status:
          type: integer
        title:
          type: string
        type:
          type: string
      required:
      - code
      - status
      - title
      - type
      type: object
info:
  description: REST API for the artifact registry. Items are keyed by (namespace,
    kind, name, version). RFC7807 Problem responses on errors.
  title: AxisML Artifact Hub API
  version: 0.1.0
openapi: 3.0.3
paths:
  /api/v1/namespaces/{namespace}/artifacts/{kind}:
    get:
      operationId: listArtifactsByKind
      parameters:
      - description: Kubernetes namespace.
        in: path
        name: namespace
        required: true
        schema:
          type: string
      - description: Artifact kind (e.g. model, dataset, code).
        in: path
        name: kind
        required: true
        schema:
          type: string
      - description: Page size (1–200, default 50).
        in: query
        name: limit
        schema:
          format: int32
          type: integer
      - description: Number of items to skip.
        in: query
        name: offset
        schema:
          format: int32
          type: integer
      - description: Optional status filter (pending, ready, failed).
        in: query
        name: status
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactList'
          description: Page.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unauthorized.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict.
        "410":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Gone.
        "412":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Precondition failed.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Service unavailable.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: List every (name, version) of a kind in a namespace
      tags:
      - artifacts
  /api/v1/namespaces/{namespace}/artifacts/{kind}/{name}:
    get:
      operationId: listArtifactVersions
      parameters:
      - description: Kubernetes namespace.
        in: path
        name: namespace
        required: true
        schema:
          type: string
      - description: Artifact kind (e.g. model, dataset, code).
        in: path
        name: kind
        required: true
        schema:
          type: string
      - description: Artifact name.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Page size (1–200, default 50).
        in: query
        name: limit
        schema:
          format: int32
          type: integer
      - description: Number of items to skip.
        in: query
        name: offset
        schema:
          format: int32
          type: integer
      - description: Optional status filter (pending, ready, failed).
        in: query
        name: status
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactList'
          description: Page.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unauthorized.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict.
        "410":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Gone.
        "412":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Precondition failed.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Service unavailable.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: List versions of an artifact
      tags:
      - artifacts
    post:
      operationId: initiateArtifact
      parameters:
      - description: Kubernetes namespace.
        in: path
        name: namespace
        required: true
        schema:
          type: string
      - description: Artifact kind (e.g. model, dataset, code).
        in: path
        name: kind
        required: true
        schema:
          type: string
      - description: Artifact name.
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArtifactInitiateInput'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactInitiateResult'
          description: Initiated.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unauthorized.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict.
        "410":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Gone.
        "412":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Precondition failed.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Service unavailable.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Initiate a new artifact version (two-phase write step 1)
      tags:
      - artifacts
  /api/v1/namespaces/{namespace}/artifacts/{kind}/{name}/{version}:
    delete:
      operationId: deleteArtifact
      parameters:
      - description: Kubernetes namespace.
        in: path
        name: namespace
        required: true
        schema:
          type: string
      - description: Artifact kind (e.g. model, dataset, code).
        in: path
        name: kind
        required: true
        schema:
          type: string
      - description: Artifact name.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Artifact version (free-form string).
        in: path
        name: version
        required: true
        schema:
          type: string
      responses:
        "202":
          description: Accepted (asynchronous deletion).
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unauthorized.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict.
        "410":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Gone.
        "412":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Precondition failed.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Service unavailable.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Delete an artifact version
      tags:
      - artifacts
    get:
      operationId: getArtifact
      parameters:
      - description: Kubernetes namespace.
        in: path
        name: namespace
        required: true
        schema:
          type: string
      - description: Artifact kind (e.g. model, dataset, code).
        in: path
        name: kind
        required: true
        schema:
          type: string
      - description: Artifact name.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Artifact version (free-form string).
        in: path
        name: version
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactView'
          description: Artifact.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unauthorized.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict.
        "410":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Gone.
        "412":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Precondition failed.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Service unavailable.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Get an artifact version
      tags:
      - artifacts
  /api/v1/namespaces/{namespace}/artifacts/{kind}/{name}/{version}/complete:
    post:
      operationId: completeArtifact
      parameters:
      - description: Kubernetes namespace.
        in: path
        name: namespace
        required: true
        schema:
          type: string
      - description: Artifact kind (e.g. model, dataset, code).
        in: path
        name: kind
        required: true
        schema:
          type: string
      - description: Artifact name.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Artifact version (free-form string).
        in: path
        name: version
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ArtifactCompleteInput'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactView'
          description: Completed artifact.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unauthorized.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict.
        "410":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Gone.
        "412":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Precondition failed.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Service unavailable.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Complete an artifact upload (two-phase write step 2)
      tags:
      - artifacts
  /api/v1/namespaces/{namespace}/artifacts/{kind}/{name}/{version}/resolve:
    get:
      operationId: resolveArtifact
      parameters:
      - description: Kubernetes namespace.
        in: path
        name: namespace
        required: true
        schema:
          type: string
      - description: Artifact kind (e.g. model, dataset, code).
        in: path
        name: kind
        required: true
        schema:
          type: string
      - description: Artifact name.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Artifact version (free-form string).
        in: path
        name: version
        required: true
        schema:
          type: string
      - description: Optional usage hint forwarded to the storage handler.
        in: query
        name: usage
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ArtifactResolveResult'
          description: Resolved.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unauthorized.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict.
        "410":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Gone.
        "412":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Precondition failed.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Service unavailable.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Resolve an artifact for download
      tags:
      - artifacts
  /healthz:
    get:
      operationId: healthz
      responses:
        "200":
          content:
            text/plain:
              schema:
                type: string
          description: ok
      summary: Liveness probe
      tags:
      - system
  /readyz:
    get:
      operationId: readyz
      responses:
        "200":
          content:
            text/plain:
              schema:
                type: string
          description: ok
        "503":
          content:
            text/plain:
              schema:
                type: string
          description: dependency not yet ready
      summary: Readiness probe
      tags:
      - system
servers:
- description: Same-origin
  url: /
tags:
- description: Artifact registry partitioned by (namespace, kind, name, version).
  name: artifacts
- description: Liveness and readiness probes.
  name: system
