# Code generated by cmd/openapi-gen. DO NOT EDIT.
# Regenerate with: make doc-gen
components:
  schemas:
    CreateTenantRequest:
      properties:
        annotations:
          additionalProperties:
            type: string
          type: object
        displayName:
          type: string
        initResources:
          allOf:
          - $ref: '#/components/schemas/ServerInitResources'
          nullable: true
        name:
          type: string
        namespace:
          $ref: '#/components/schemas/ServerNamespaceSpec'
        quotas:
          items:
            $ref: '#/components/schemas/ServerQuotaSpec'
          type: array
        suspended:
          type: boolean
      type: object
    ListTenantsResponse:
      properties:
        continue:
          type: string
        items:
          items:
            $ref: '#/components/schemas/ServerTenantResponse'
          type: array
      required:
      - items
      type: object
    PatchTenantRequest:
      properties:
        annotations:
          additionalProperties:
            type: string
          type: object
        displayName:
          nullable: true
          type: string
        initResources:
          allOf:
          - $ref: '#/components/schemas/ServerInitResources'
          nullable: true
      type: object
    Problem:
      properties:
        detail:
          type: string
        status:
          type: integer
        title:
          type: string
        type:
          type: string
      required:
      - status
      - title
      - type
      type: object
    QuotaListResponse:
      properties:
        spec:
          items:
            $ref: '#/components/schemas/QuotaSpec'
          type: array
        status:
          items:
            $ref: '#/components/schemas/ServerQuotaStatus'
          type: array
      required:
      - spec
      - status
      type: object
    QuotaSpec:
      properties:
        max:
          additionalProperties:
            type: string
          type: object
        min:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
        pool:
          type: string
      type: object
    ServerConfigMapSpec:
      properties:
        name:
          type: string
        sourceConfigMapRef:
          $ref: '#/components/schemas/ServerObjectRef'
      required:
      - name
      - sourceConfigMapRef
      type: object
    ServerImagePullSecretSpec:
      properties:
        name:
          type: string
        sourceSecretRef:
          $ref: '#/components/schemas/ServerObjectRef'
      required:
      - name
      - sourceSecretRef
      type: object
    ServerInitResources:
      properties:
        configMaps:
          items:
            $ref: '#/components/schemas/ServerConfigMapSpec'
          type: array
        imagePullSecrets:
          items:
            $ref: '#/components/schemas/ServerImagePullSecretSpec'
          type: array
        secrets:
          items:
            $ref: '#/components/schemas/ServerSecretSpec'
          type: array
        serviceAccounts:
          items:
            $ref: '#/components/schemas/ServerServiceAccountSpec'
          type: array
      type: object
    ServerNamespaceSpec:
      properties:
        annotations:
          additionalProperties:
            type: string
          type: object
        labels:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
      required:
      - name
      type: object
    ServerObjectRef:
      properties:
        name:
          type: string
        namespace:
          type: string
      required:
      - name
      - namespace
      type: object
    ServerQuotaSpec:
      properties:
        max:
          additionalProperties:
            type: string
          type: object
        min:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
        pool:
          type: string
      required:
      - max
      - name
      - pool
      type: object
    ServerQuotaStatus:
      properties:
        message:
          type: string
        name:
          type: string
        pool:
          type: string
        ready:
          type: boolean
        used:
          additionalProperties:
            type: string
          type: object
      required:
      - name
      - pool
      - ready
      type: object
    ServerRBAC:
      properties:
        roleRef:
          allOf:
          - $ref: '#/components/schemas/ServerRoleRef'
          nullable: true
        rules:
          items:
            additionalProperties: {}
            type: object
          type: array
      type: object
    ServerRoleRef:
      properties:
        kind:
          type: string
        name:
          type: string
      required:
      - kind
      - name
      type: object
    ServerSecretSpec:
      properties:
        name:
          type: string
        sourceSecretRef:
          $ref: '#/components/schemas/ServerObjectRef'
        type:
          type: string
      required:
      - name
      - sourceSecretRef
      type: object
    ServerServiceAccountSpec:
      properties:
        imagePullSecrets:
          items:
            type: string
          type: array
        name:
          type: string
        rbac:
          allOf:
          - $ref: '#/components/schemas/ServerRBAC'
          nullable: true
      required:
      - name
      type: object
    ServerTenantResponse:
      properties:
        createdAt:
          format: date-time
          type: string
        displayName:
          type: string
        id:
          type: string
        initResources:
          allOf:
          - $ref: '#/components/schemas/ServerInitResources'
          nullable: true
        name:
          type: string
        namespace:
          $ref: '#/components/schemas/ServerNamespaceSpec'
        quotas:
          items:
            $ref: '#/components/schemas/ServerQuotaSpec'
          type: array
        status:
          $ref: '#/components/schemas/ServerTenantStatus'
        suspended:
          type: boolean
      required:
      - id
      - name
      - namespace
      - status
      - suspended
      type: object
    ServerTenantStatus:
      properties:
        message:
          type: string
        phase:
          type: string
        quotas:
          items:
            $ref: '#/components/schemas/ServerQuotaStatus'
          type: array
      type: object
    TenantResponse:
      properties:
        createdAt:
          format: date-time
          type: string
        displayName:
          type: string
        id:
          type: string
        initResources:
          allOf:
          - $ref: '#/components/schemas/ServerInitResources'
          nullable: true
        name:
          type: string
        namespace:
          $ref: '#/components/schemas/ServerNamespaceSpec'
        quotas:
          items:
            $ref: '#/components/schemas/ServerQuotaSpec'
          type: array
        status:
          $ref: '#/components/schemas/ServerTenantStatus'
        suspended:
          type: boolean
      required:
      - id
      - name
      - namespace
      - status
      - suspended
      type: object
info:
  description: Stateless REST shell over Tenant CR CRUD. Forwards to the Kubernetes
    API server. RFC7807 Problem responses on errors.
  title: AxisML Cluster Manager API
  version: 0.1.0
openapi: 3.0.3
paths:
  /api/v1/tenants:
    get:
      operationId: listTenants
      parameters:
      - description: Page size for the K8s LIST call.
        in: query
        name: limit
        schema:
          format: int32
          type: integer
      - description: K8s LIST continue token from a prior page.
        in: query
        name: continue
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ListTenantsResponse'
          description: Tenant page.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: List tenants
      tags:
      - tenants
    post:
      operationId: createTenant
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateTenantRequest'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantResponse'
          description: Tenant created.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Create a tenant
      tags:
      - tenants
  /api/v1/tenants/{name}:
    delete:
      operationId: deleteTenant
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: Deleted (idempotent).
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Delete tenant
      tags:
      - tenants
    get:
      operationId: getTenant
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantResponse'
          description: Tenant.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Get tenant
      tags:
      - tenants
    patch:
      operationId: patchTenant
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchTenantRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantResponse'
          description: Updated tenant.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Patch tenant
      tags:
      - tenants
  /api/v1/tenants/{name}/quotas:
    get:
      operationId: listQuotas
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QuotaListResponse'
          description: Quota spec + status.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: List quotas of a tenant
      tags:
      - quotas
    post:
      operationId: addQuota
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuotaSpec'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantResponse'
          description: Updated tenant.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Add a quota to a tenant
      tags:
      - quotas
  /api/v1/tenants/{name}/quotas/{pool}/{quota}:
    delete:
      operationId: deleteQuota
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Resource pool name.
        in: path
        name: pool
        required: true
        schema:
          type: string
      - description: Quota name.
        in: path
        name: quota
        required: true
        schema:
          type: string
      responses:
        "204":
          description: Deleted (idempotent).
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Delete a quota
      tags:
      - quotas
    patch:
      operationId: updateQuota
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Resource pool name.
        in: path
        name: pool
        required: true
        schema:
          type: string
      - description: Quota name.
        in: path
        name: quota
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/QuotaSpec'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantResponse'
          description: Updated tenant.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Update a quota
      tags:
      - quotas
  /api/v1/tenants/{name}/suspend:
    post:
      operationId: suspendTenant
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantResponse'
          description: Suspended.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Suspend tenant
      tags:
      - tenants
  /api/v1/tenants/{name}/unsuspend:
    post:
      operationId: unsuspendTenant
      parameters:
      - description: Tenant name (metadata.name).
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TenantResponse'
          description: Unsuspended.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Validation error.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Forbidden.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Conflict (already exists or concurrent modification).
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Internal error.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Problem'
          description: Unexpected error.
      summary: Unsuspend tenant
      tags:
      - tenants
  /healthz:
    get:
      operationId: healthz
      responses:
        "200":
          description: ok
      summary: Liveness probe
      tags:
      - system
  /readyz:
    get:
      operationId: readyz
      responses:
        "200":
          description: ok
      summary: Readiness probe
      tags:
      - system
servers:
- description: Same-origin
  url: /
tags:
- description: Tenant CRUD over the Tenant CR via the K8s API.
  name: tenants
- description: Per-tenant ElasticQuota declarations (spec.quotas[]).
  name: quotas
- description: Liveness and readiness probes.
  name: system
